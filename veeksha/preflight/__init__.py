"""Preflight package.

The measurement-fidelity checks land with the preflight commit; this package
currently hosts the shared sharded mock-server infrastructure
(``sharded_server``) used by the audio/text mock servers and test helpers.
"""
