[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra --strict-markers
markers =
    unit: Unit tests
    functional: Functional tests
    gpu: Tests that require GPU
    no_vllm_server: GPU tests that manage their own server lifecycle and do not use the shared vLLM fixture
    slow: Slow / informational tests (e.g. timing-drift knee reports); may be deselected in CI
filterwarnings =
    ignore::DeprecationWarning
    ignore::PendingDeprecationWarning